# Terraform state files (in all folders)
*.tfstate
*.tfstate.backup

# Terraform crash logs
crash.log

# Terraform plan files
*.tfplan

# Local Terraform directories (recursively ignores all .terraform folders)
.terraform/

# Variable files (secrets, local configs)
*.tfvars
*.tfvars.json

# Terraform override files (used for local-only configs)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Optional: lock file (if you don't want to track versions globally)
# .terraform.lock.hcl
